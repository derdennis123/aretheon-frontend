{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo \">> running prisma db push\" && node node_modules/prisma/build/index.js db push --accept-data-loss --schema=prisma/schema.prisma && echo \">> starting next server on $PORT\" && node server.js",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
